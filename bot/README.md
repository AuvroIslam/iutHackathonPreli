# Discord Bot (`@office/bot`)

Discord bot (discord.js) that answers `!status` / `!room` / `!usage` from the shared backend and posts proactive alerts. Scaffolded as a workspace placeholder — implementation lands in the Bot milestone. See [`../prd.md`](../prd.md) §11.
