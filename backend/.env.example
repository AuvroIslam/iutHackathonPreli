# Backend configuration (copy to .env; .env is git-ignored)

# HTTP port the REST API / Socket.IO server listens on
PORT=4000

# Simulation tick interval in milliseconds (higher = calmer; raise for a demo)
SIM_TICK_MS=6000
