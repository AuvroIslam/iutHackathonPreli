# Base URL of the backend for production builds (Vite inlines this at build time).
# Leave empty for local dev — `npm run dev` proxies /api and the socket to
# http://localhost:4000 via vite.config.ts.
# In production set it to your hosted backend, e.g. https://office-backend.onrender.com
VITE_BACKEND_URL=
